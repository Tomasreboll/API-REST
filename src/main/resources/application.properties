spring.application.name=alura challenge API Rest

spring.datasource.url=jdbc:mysql://localhost:3006/${DB_NAME_MYSQL}
spring.datasource.username=${DB_USER_MYSQL}
spring.datasource.password=${DB_PASSWORD_MYSQL}

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

api.security.secret=${JWT_SECRET:789456}


server.error.include-stacktrace=never

